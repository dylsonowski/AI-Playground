enum class LOGGING_METHOD {
	BASIC = 0,
	FILE_AND_TERMINAL,
	FILE_ONLY,
	TERMINAL_ONLY
};

enim

struct LogManagerConfig {
    LOGGING_METHOD loggingMethod = LOGGING_METHOD::BASIC,
    std::vector
};
