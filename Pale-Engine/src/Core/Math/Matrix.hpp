#pragma once

namespace Pale::Core::Math {
	class Matrix {
	public:
		Matrix(/* args */);
		~Matrix();

    private:
        uint16_t _width;
        uint16_t _height;
		
	};
} // namespace Pale::Core::Math