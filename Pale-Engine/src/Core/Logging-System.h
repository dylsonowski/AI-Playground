#pragma once

namespace Pale::Core {
	enum class LOGGING_METHOD {
		BASIC = 0,
		FILE_AND_TERMINAL,
		FILE_ONLY,
		TERMINAL_ONLY
	};

	class Logging_System {

	public:
	private:
	};
} // namespace Pale::Core